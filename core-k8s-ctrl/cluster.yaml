apiVersion: k0smotron.io/v1beta1
kind: Cluster
metadata: # kpt-merge: k0smotron/example-cluster
  name: example-cluster
  namespace: k0smotron
  annotations:
    internal.kpt.dev/upstream-identifier: 'k0smotron.io|Cluster|k0smotron|example-cluster'
spec:
  k0sVersion: k0s_Release_Version # kpt-set: ${k0s-release-version}
  persistence:
    type: emptyDir
  service:
    type: LoadBalancer
